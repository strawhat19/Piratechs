import RoutePage from '@/app/components/page-shell/route-page';

export const metadata = {
  title: `Case Studies // Piratechs`,
};

export default function CaseStudiesPage() {
  return <RoutePage pageID={`projects`} />;
}
